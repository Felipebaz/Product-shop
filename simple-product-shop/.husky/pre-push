#!/usr/bin/env sh

# Git runs hooks from the repository root, but package.json lives one level in.
cd "$(dirname "$0")/.." || exit 1

echo "🚀 Running pre-push checks..."

echo "🧪 Running tests..."
pnpm test:run || exit 1

echo "📦 Building..."
pnpm build || exit 1

echo "✅ Pre-push checks passed!"
